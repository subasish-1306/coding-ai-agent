# coding: utf-8
"""Placeholder for SQLAlchemy ORM models.

Future models should inherit from `app.database.Base`:

    from app.database import Base
    from sqlalchemy import Column, Integer, String

    class User(Base):
        __tablename__ = "users"
        id = Column(Integer, primary_key=True)
        ...
"""
